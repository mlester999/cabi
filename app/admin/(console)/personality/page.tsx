import { CabiStatusMessagesPanel } from "@/components/admin/cabi-status-messages-panel";
import { ConfigPanel } from "@/components/admin/config-panel";
export default function PersonalityPage() { return <><ConfigPanel configKey="personality" eyebrow="How Cabi speaks" title="Personality" description="Shape Cabi's warmth and voice inside a fixed safety envelope that cannot be overridden." fields={[{ key: "systemPrompt", label: "Cabi system prompt", kind: "textarea" }, { key: "greeting", label: "First greeting", kind: "textarea" }, { key: "traits", label: "Personality traits" }, { key: "allowedNickname", label: "Allowed nickname" }, { key: "tone", label: "Tone" }, { key: "catExpressionFrequency", label: "Cat-expression frequency" }, { key: "defaultMood", label: "Default mood" }, { key: "memoryBehavior", label: "Memory behavior", kind: "textarea" }]} /><CabiStatusMessagesPanel /></>; }
