import { ConfigPanel } from "@/components/admin/config-panel";
export default function AdminSettingsPage() { return <ConfigPanel configKey="app_config" eyebrow="Operations" title="App settings" description="Control server-side product behavior without exposing secrets to the browser." fields={[{ key: "knowledgeSource", label: "Knowledge source", kind: "url" }, { key: "memoryExtraction", label: "Explicit memory extraction", kind: "boolean" }, { key: "summaries", label: "Conversation summaries", kind: "boolean" }, { key: "adminConversationAccess", label: "Allow audited admin conversation access", kind: "boolean" }]} />; }
