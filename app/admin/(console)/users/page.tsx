import { AdminDataTable } from "@/components/admin/admin-data-table";
export default function UsersPage() { return <AdminDataTable resource="users" title="Users" description="Anonymous guest profiles and returning Cabi visitors. Only minimum profile data is stored." />; }
