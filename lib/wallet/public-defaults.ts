/**
 * Owner-supplied $CPU copy that is safe to ship in the repository.
 *
 * IMPORTANT: no contract address and no Clank.trade link live here. Those are
 * published values that must be entered by the owner — either in
 * `/admin/cpu` or through the `CPU_CONTRACT_ADDRESS` / `CPU_CLANK_TRADE_URL`
 * deployment variables — so the repository can never present an unverified
 * token destination as official. The product rules and `README.md` both require
 * a contract to be independently verified before it is shown publicly.
 */

/** Neutral product copy used until the owner writes their own description. */
export const fallbackCpuDescription =
  "The community token for Cabi, the Cat Partner Unit. Cabi's chat, memory, and wallet features keep taking shape while the token details below stay owner-published.";
